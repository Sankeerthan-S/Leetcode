class Solution {
    public int arrayPairSum(int[] nums) {
        Arrays.sort(nums);
        int n=nums.length;
        int c=0,i=0;
        while(i<n)
        {
            c=c+nums[i];
            i+=2;
        }
        return c;

    }
}
